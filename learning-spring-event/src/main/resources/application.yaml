#server.port=8080
#
#spring.application.name=learning-spring-event

server:
  port: 8080

spring:
  application:
    name: learning-spring-event
  datasource:
    url: jdbc:mysql://localhost:3306/demo?characterEncoding=UTF8&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver


mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.fxg.learning.event.domain

